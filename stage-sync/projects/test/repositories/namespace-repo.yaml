apiVersion: integrations.k8smgmt.io/v3
kind: Repository
metadata:
  name: namespace-repo
  project: test
spec:
  agents:
  - {}
  credentials:
    password: sealed://credentials.password
    username: gajanan-rafay
  endpoint: https://github.com/gajanan-rafay/examples
  options: {}
  secret:
    name: file://artifacts/namespace-repo/sealed-secret.yaml
  type: Git
