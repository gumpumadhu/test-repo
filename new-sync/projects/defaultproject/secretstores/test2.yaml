apiVersion: integrations.k8smgmt.io/v3
kind: SecretStore
metadata:
  name: test2
  project: defaultproject
spec:
  config:
    vault:
      clusters:
      - authPath: test
      host: asdgfasdf
  provider: Vault
